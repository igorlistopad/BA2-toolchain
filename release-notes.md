BA2 cross-toolchain prerelease.

Included compiler-only archives:

- macOS ARM64
- macOS AMD64
- Linux AMD64

The host build uses Clang and produces the `ba-elf` binutils + GCC/newlib
toolchain for the BA2 target.
